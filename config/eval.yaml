# Corpus evaluation + ablation. Stages run cumulatively from greedy CTC to full LM fusion so the
# report shows each component's WER/CER/RTF/latency contribution.
ablation_stages: ["ctc_greedy", "prefix_beam", "attn_rescore", "lm_rescore", "lm_fusion"]
report_path: runs/eval/report.json
